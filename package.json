{
  "name": "notecode",
  "private": true,
  "workspaces": [
    "client",
    "backend"
  ],
  "engines": {
    "node": ">=20.15.1",
    "npm": ">=10.8.2"
  },
  "scripts": {
    "start:client": "npm --workspace client run dev",
    "start:backend": "npm --workspace backend run dev"
  }
}
